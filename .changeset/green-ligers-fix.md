---
"@ai-hero/sandcastle": patch
---

Fix completion-timeout and visible-inactivity edge cases, validate heartbeat and visible-inactivity timer values, and restore `createSandbox().run()` / `createWorktree().run()` timing-option parity with `run()`.
