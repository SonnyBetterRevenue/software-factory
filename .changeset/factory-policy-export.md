---
"@ai-hero/sandcastle": patch
---

Publish the Software Factory policy helpers as `@ai-hero/sandcastle/factory-policy`, and build `dist` during `prepare` so exact Git-commit consumers can import the subpath.
